import 'package:flutter/material.dart';

class MasjidInfoTile extends StatelessWidget {
  const MasjidInfoTile({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const ListTile();
  }
}
